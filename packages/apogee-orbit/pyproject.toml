[project]
name = "apogee-orbit"
version = "0.1.0"
description = "Orbital propagation and yaw steering (future implementation)"
readme = "README.md"
authors = [
    { name = "Dax", email = "daxnavarrete@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "jax",
]

[build-system]
requires = ["uv_build>=0.9.21,<0.10.0"]
build-backend = "uv_build"
