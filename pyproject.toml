[project]
name = "apogee"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apogee-core",
    "diffrax>=0.7.0",
    "jax>=0.8.2",
    "optimistix>=0.0.11",
    "scipy>=1.17.0",
    "ussa1976>=0.3.4",
]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
apogee-core = { workspace = true }
